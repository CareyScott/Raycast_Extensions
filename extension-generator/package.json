{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "extension-generator",
  "title": "Extension Generator",
  "description": "Generate new Raycast extensions using Claude Code. Analyzes your existing extensions and creates new ones based on your prompts.",
  "icon": "icon.png",
  "author": "scottcarey",
  "categories": [
    "Developer Tools",
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Generate Extension",
      "subtitle": "Extension Generator",
      "description": "Generate a new Raycast extension using Claude Code CLI",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "claudePath",
      "type": "textfield",
      "required": false,
      "title": "Claude CLI Path",
      "description": "Path to Claude Code CLI executable (optional, will auto-detect if not specified)",
      "placeholder": "/opt/homebrew/bin/claude"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.64.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.8",
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "eslint": "^8.55.0",
    "prettier": "^3.1.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
